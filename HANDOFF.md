# HistoryLab — Session Handover (read this first)

> **📱 LANDING MOBILE "PACKAGE 1" — ON `dev` 2026-07-28 (`ae092fe`), AWAITING UDAY REVIEW → merge to main.** From a full 390px designer review (12 slices, zero h-overflow, page 9.6k px). Shipped mobile-only (≤800px, desktop verified pixel-unchanged at 1280): **sticky bottom CTA bar** (IntersectionObserver on the hero button itself, rootMargin -56px for the header — fires the moment "Start learning free" slides off/under the header, Uday's ask; hides when final-CTA band/footer visible), **slim header** (brand + Sign in only; Start-free hidden — hero CTA + sticky bar own it), **"See pricing ↓" jump link** under hero CTA, **₹499 card leads the pricing stack** (CSS `order:-1`). **Phone-frame hero was built then REVERTED — Uday wants the iPad render kept on mobile too** (hero-phone.png deleted; screenshot lesson stands: devpreview deep-link routes are `/chapter/ch1` not `c10-hist-ch1` — click through from shelf). **HERO POLISH (same day, from an external-AI mockup Uday shared — steal/adapt/refuse deliberation, he green-lit steals 1–5 + DESKTOP HERO NOW FAIR GAME):** kicker → tinted pill (mobile: deliberate 2-line break between phrases), explicit 2-line H1 (desktop 60→54px so "finally understood." holds one line), sub-paragraph + emoji creds MERGED into 3 line-icon proof rows (copy verbatim, statement+its proof paired), micro+jump → icon chip row "First section free · no card needed | See pricing →" (both breakpoints), CSS tilt on existing iPad img (-5° desktop / -4° mobile). **REFUSED from that mockup (locked copy rules): "examiner-style feedback" (differentiator is a REAL examiner), "Try a section free" (locked: first-section language), Start-free back in mobile header, fabricated app UI in device shots.** Parked from it: features band → horizontal chip strip on mobile (Package 2), Red Fort line-art hero bg (asset task). **DEFERRED (analysed, Uday chose Package 1 only — "Package 2" findings if he asks later):** problem-trio compaction, features-grid 1-col, comparison-table stacking, eyebrow pill split, trust-chip 2×2, emoji→line-icon sweep.
>
> **📱 MOBILE RESPONSIVE v1 — LIVE ON PROD 2026-07-28 (`e81bab9`, branch `mobile-tabbar` merged → dev → main).** Desktop pixel-unchanged (all lg:-guarded). Shipped: **bottom tab bar** Learn·Revise·Practice·Exam Prep (WorkspaceShell-only → auto-hidden in reader/quiz/player/Shelf; 🔒 on Practice for preview users); **Revise hub** `/chapter/:cid/revise` (Uday's 4 infographics as transparent cutouts in `public/images/revise-*.png`, neutral white cards, real counts); **app bar** = ☰ + brand + ⭐stars + avatar (CH pill retired; avatar opens drawer); **mobile crumbs** = single parent back-link; **Learn tab** = hero (art@45%, %·bar·sections progress row, borderless tappable chips, Start/Continue CTA + next-topic caption) → journey → quote (rail progress/mock-CTA/stats desktop-only; preview UNLOCK band kept both breakpoints); **Section page** = hero cockpit (progress row + CTA→next topic→quiz→completed chip) → "What You'll Learn" slim band (own card, collapsed, Uday's sample) → sheet tabs **Topics | Quiz & Explore** (NEVER "Practice" — bottom-bar word; smart default to Quiz&Explore when only quiz left; rail quiz/mock/ring desktop-only, Order-events+section-Flashcards live in the tab). **NEXT (Uday): Tools + Mock Tests pages get the same treatment AFTER this merge.** Screenshot tooling: bypass patch + `?devpreview=1` on vite :5199, puppeteer 390px. **PROCESS (reinforced): "can we X?" = deliberate, NOT a go-ahead — build only after his explicit go.**
>
> **⏭ POST-LAUNCH STRUCTURE (Uday, 2026-07-27 sign-off): work runs on TWO TRACKS from 2026-07-28.**
> **Track 1 — Adoption & Marketing:** getting real students/parents in (pilot-cohort conversion, positioning, channels, Neha's network, quotes/social proof once real users bite). Fresh territory — no plan exists yet; start by drafting one with Uday.
> **Track 2 — Product & Content:** remaining Class-10 chapters (2 live, 3 in the works per landing), content rounds with Neha, deferred enhancements (P1 polish, parent/guardian email, boardYear pills, Ch2 S2 figures, summary-row leftovers) + the small post-launch tail in the LIVE block below (₹149 live smoke, webhook-secret rotation, `SHOW_PILOT_QUOTES`).
> Keep the tracks separate in sessions; BIGGER_PICTURE.md holds the multi-class roadmap Track 1 should eventually feed.

**Last updated:** 2026-07-27 · **Branch:** `dev`

> **🎉 LIVE WITH REAL MONEY — 2026-07-27 ~22:06 IST.** Smoke verified in prod DB: nehud2017 bought c10-hist-ch1 for **₹499 real** (purchase paid → entitlement granted → `payment.captured` webhook delivered + HMAC-verified). Live keys + fresh webhook secret set via `supabase secrets set` (test keys retired; the placeholder-overwrite mishap was fixed by re-setting real values). Prices repriced ₹499/₹799 (Neha, decision: her call). **Pre-launch data wipe done via `supabase db query --linked`:** purchases/payment_events/examiner_reviews/purchase-entitlements zeroed, 5 admin grants kept (note: nehud2017's access now purchase-sourced from the real ₹499). `razorpay-webhook-live` clone endpoint deployed as URL-collision fallback (may be unused — the original webhook verified fine; confirm which URL carries the live secret, then optionally delete the spare). **PENDING to fully close:** (1) real ₹149 examiner-review purchase + Neha marks it (last untested live rail); (2) Neha's personal paywall heads-up to pilot cohort; (3) post-launch hygiene: rotate webhook secret (travelled through chat/shell history), day-80 reminder on Razorpay 90-day zero-fee offer, real student quotes → flip `SHOW_PILOT_QUOTES`.
>
> *(Superseded launch-day narrative:)* dev→main `408588f` + follow-ups (auth wake-fix `50e1468`, marked-state results `c85548d`, scheme=paid-review-only `3f8b62c` + per-answer nudge `8c5755c`/`59b4811`) all live on historylab.in; pilot-quotes band hidden; legal/SEO final.

> **🔒 DECISION #40 (2026-07-27, Uday): NO REFUNDS AT ALL — chapters included** (supersedes the 7-day chapter refund from Sprint 3 era). Refund Policy page = all purchases final + failed/duplicate-payment carve-out (payment errors reversed via Razorpay; not a "refund"). Landing: refund chip → "Try free before you pay"; FAQ answer = the free section IS the trial. Also same session: **sample-quotes note removed** (⚠️ the Ananya/Rohan quotes are still INVENTED and now carry no disclaimer — swap in real pilot quotes before launch or hide the band), **footer parent-subscribe column REMOVED** (3-col grid; further footer feedback coming from Uday separately), **examiner card remark rewritten (Option B) + "returned within 72 hours"**, and the full **honest-marking landing sweep** (no "marked in seconds"/"engine" anywhere; differentiator repositioned to "a real CBSE examiner marks you"; 48–72h → "within 72 hours" everywhere).
>
> **🚀 GO-LIVE — ONLY 4 STEPS LEFT (state as of 2026-07-27 15:30 IST):** (0) **Supabase Site URL → https://historylab.in + purge stale 192.168.x redirects — UNCONFIRMED, Uday dashboard**; (1) **Neha's E2E on prod** (test card 4111…, 5-step checklist in Milestone B block below; her Admin→Pricing change = repricing both chapters to **launch ₹499 / list ₹799 — HER ACTION, server prices still ₹199 until she does**); (2) **live keys** (Uday generates, Claude sets `supabase secrets set` + live webhook: same function URL, payment.captured + refund.processed, new secret — test-mode webhook does NOT fire for live payments); (3) **real smoke purchase ₹499** + webhook delivered. **CLOSED TODAY:** legal final (Pi By Two entity, info@teknomatics.com everywhere, governing-law restored, GST → "receipt via Razorpay, GST invoice on request" + landing chip/bullet/FAQ aligned `9d7cf0b`); backups LIVE (nightly 02:30 IST → R2 `historylab-backups`, verified object in daily/; ubuntu-24.04 apt-key fix `604bf1c` + PG 17.6 needs `/usr/lib/postgresql/17/bin/pg_dump` directly `6d8ca51`); paywall heads-up = Neha communicates personally (no drafted blast); papers = 3 enough for launch (Neha), more with future chapters. **PARKED: real student quotes in a few weeks once students use it — `SHOW_PILOT_QUOTES=false` stays; faking quotes REFUSED (fabricated testimonials).** **(C) Deferred:** parent email/guardian verify, polish round + P1s, Ch2 S2 figure items, boardYear, summary-row removal.

> **🚧 MILESTONE B — EXAMINER REVIEW SHIPPED (`0062074`), E2E PENDING (decision #39, locked with Uday+Neha 2026-07-27):** **NO AI marking — ever (supersedes #31's "explore later"); Neha hand-marks every ₹149 review.** Terms: per-ATTEMPT purchase (product `examiner-review`), **no cap** (queue shows waiting-days; SLA copy "within 72 hours"), **NO refunds** (disclaimer on the band; a forced dashboard refund still coheres: unmarked→refunded, marked stays), in-app notification only, edit-after-publish allowed, **"the paper stays always"** (RESTRICT FK + upsert/delete guards — a paper with paid reviews can never be replaced or deleted). Marks live on `examiner_reviews.marks` jsonb (never fabricated answers rows). **DEPLOYED: migration `20260727120000` pushed + all four functions redeployed (webhook --no-verify-jwt), anon probes 401.** Student flow: result page = violet ₹149 band → "With the examiner" chip → marked state (examiner total, overall note, per-question 🖋️ marks+comments); Test Centre chips now MCQs marked / With examiner / Examiner-marked (old "Marked ✓" overclaim retired). Admin → **Examiner tab**: queue (oldest-first, waiting-days red ≥2d) + workbench (answer beside scheme, half-mark steps, Save draft / Publish / Re-publish). **E2E CHECKLIST (Uday/Neha, test keys `rzp_test_TErf7Eboldb6AT`):** (1) student account: attempt a paper → result → Submit for review ₹149 → Razorpay test card → band becomes "With the examiner"; (2) Neha on `/admin` → Examiner → mark + publish → student reloads result → examiner total + comments; (3) re-publish an edit; (4) try re-uploading that paper in Papers tab → expect the friendly 409. **PARKED (Uday): remove the per-section summary row on results (he says not required — decide after E2E).** NOTE: `nehud2017@gmail.com` is Neha's STUDENT test account; her admin work needs a seeded admin (udaysharma10/uday@teknomatics/nehaudaysharma).

> **✅ NEHA ROUND 2 FIXES (`f65d62a`):** (1) Result page per-section row now shows EVERY paper section — written-only sections (B/C/D on the ch1 paper) were filtered out by the MCQ-marks-only bars and read as missing (her nehud2017 attempt); they now render "N written · check the scheme below" while MCQ sections keep scored bars. (2) Chapter-home rail "Quick practice" block removed entirely (Flashcards card duplicated the sidebar). **NEXT UP (Uday): the Examiner-review track** — decision #31 (AI marking on hold, Neha revisiting) + the ₹149 examiner add-on (Milestone B). Note for that discussion: attempts/answers schema blocks nothing; examiner_reviews table exists since Sprint 1; "MARKED ✓"→"MCQs marked" wording fix still open (P1).

> **✅ REVISE-TOOLS PASS SHIPPED 2026-07-27 (`4fa02de`) — Neha's rulings on the nl-04 deliberation (mockup `9be12cf`):** **Timeline** = explorer-only (lobby gone); "EXAM IMPORTANT" → **"BOARD Q" pill** (KeyDate gained optional `boardYear` → renders "BOARD Q · 2019"; **OPEN: Neha to author boardYear per event** — pills show plain "BOARD Q" until then; her Exam-only filter toggle idea was REFUSED by her, not built). **"Order the events" moved to the section rail** (her ask): shows only for sections with activities (ch1 s1–s4), deep-links `/timeline?practice=<sid>`, exit/retry return to the section, completion credits the practised section (was hardwired s1). **Flashcards: UNCHANGED (her call — she likes it; nl-04's flashcard concept dropped).** **Maps:** kept as-is minus the "All Map Activities" list. **Figures:** gallery IS the page (lobby gone); compact "Practice: image analysis" header button kept so the 10 image-MCQs stay reachable — **flagged: one line to remove if Neha wants it gone entirely.** All render-verified (shots `y01–y05` in `../HistoryLab_Design_Review_2026-07-26/`).

> **✅ IA PASS SHIPPED 2026-07-27 (`5e0b9ff`, on `dev`) — Neha's four-concept sidebar + section tree + full crumbs.** Sidebar groups: **LEARN (Overview) / REVISE (Flashcards·Timeline·Maps·Figures) / PRACTICE (Mock Tests) / EXAM PREP (Exam Prep, own heading)** — the `/exam` page is "Exam Prep" again everywhere; NOTHING is named "Practice" anymore; section-page quiz renamed **"Section Quiz"**. **Section tree is location-driven** (Uday rejected always-visible as overwhelming and collapsible as undiscoverable): LEARN auto-expands to show all sibling sections (current highlighted) ONLY while inside a section; folded on Overview/tools (journey list already lists sections there). **Crumb grammar unified: `All Chapters › {chapter title} › {page}`** (Uday explicitly superseded #37's two-segment cap). **Altitude rule: sidebar = chapter, rail = current page** — section rail now offers "Section Quiz · completes this section" + this-section Flashcards (`/flashcards?section=sX` auto-starts that section's practice; one-shot flag set inside the timer callback for StrictMode); chapter rail dropped the "Practice questions" card (sidebar duplication, Uday). **Screenshot-bypass tooling saved as `../HistoryLab_Design_Review_2026-07-26/devpreview-bypass.patch`** — `git apply` it for future headless UI reviews (auth+bundle+test-engine mocks; NEVER commit it), screenshots via puppeteer-core `snap.mjs` pattern (virtual-time freezes framer-motion).

> **✅ P0 COHERENCE PASS SHIPPED 2026-07-26 (`7c4282a`, on `dev`, NOT pushed/merged) — after a full 26-screenshot UI review (Uday-requested; findings + shots in `../HistoryLab_Design_Review_2026-07-26/REVIEW.md`).** One-name law applied: **"Mock Tests"** everywhere (Test Centre retitled; kicker's "Study Tools" vocabulary killed), **"Practice"** page title matches sidebar (was "Exam Prep"), **"Back to Home" eliminated app-wide** (tool pages lost their duplicate in-pane back-link + emoji tile headers; error fallbacks say "All Chapters"). WorkspaceLayout: tool crumbs are now SINGLE-segment (tools are siblings of Overview, not children); results = "Mock Tests > Result"; AttemptResult's second crumb row removed (paper title moved into hero meta). **Sidebar finally implements spec §1:** `‹ All Chapters` + chapter pill on top, profile block pinned to bottom (desktop aside = 100dvh), 🔒 badge on Mock Tests for un-entitled users. Immersion grammar unified: QuizMode entry uses the reader-style sticky top bar; **PaperPlayer got "Save & exit"** (a live attempt previously had NO exit). **Review correction:** preview users' journey-row locks were ALWAYS fine (🔒 Unlock pills, "Section 1 free" chip — the review's contrary shot was a mock artifact; locks derive from the server bundle's `preview` marker). **P1 items deferred to Neha's polish round:** emoji→line-icon sweep on tool/quiz cards, "MARKED ✓"→"MCQs marked", scheme "+1" chips restyled so they don't read as awarded marks, shelf V2 restyle, TestCentre/player/results deep retints. Screenshot tooling note: framer-motion pages render blank under Chrome `--virtual-time-budget` — use puppeteer-core + real waits (`scratchpad/snap.mjs` pattern).

> **🚧 PHASE 2 BUILD — Stages 1+2 SHIPPED 2026-07-26 (`bbc993f`→`65b5eec`), Stage 3 (propagation) NEXT:** V2 tokens (`v2-*` in globals.css, additive), shell components (`src/components/shell/`: Sidebar with LEARN/PRACTICE/REVISE + profile menu, WorkspaceShell 3-col + mobile drawer, WorkspaceLayout auto-crumb wrapper for tool pages), **topic routes live** (`/chapter/:cid/section/:sid/topic/:i` 1-based, TopicReader→NarrativeMode; SectionModule navigates, no more component-state reader). **Ported: Overview** (HomePage — light chip hero + resume band + journey stepper + rail w/ ring/prime/stats; old #34 bands retired), **Section page** (dark hero w/ per-section SECTION_ART map, sheet = Learn→Topics→Practice&Explore w/ SectionTimeline/SectionMaps embedded), **Reader chrome** (NarrativeMode sticky immersion bar; card engine untouched). Time estimates computed (cards×45s, quiz 5min); streak stat dropped (not tracked). **Stage 3 coherence pass SHIPPED (`0c5dcfb`):** TestCentre internal breadcrumb removed + "objective"→"MCQ" wording law applied (TestCentre + AttemptResult), QuizMode crumb→single immersion exit. Uday verified Stages 1+2 on localhost ("works fine"). **REMAINING (deferred to Neha's page-by-page minor-polish round, her call):** deep visual retints of TestCentre/results/quiz/player internals (still legacy peach tokens), tool-page in-pane headers, BookHome kept as-is (Uday 2026-07-26), landing seam check. NOTE: pre-existing eslint error in QuizMode (react-hooks/preserve-manual-memoization) — not from this work.
> **✅ DESIGN LANGUAGE V2 LOCKED (2026-07-26, decision #38):** Neha approved altitude nav + content flow on `mockups/nl-03-flow.html` v2. Minor design notes deferred to page-by-page polish AFTER the build. #36 questions resolved as rendered: light hero on chapter home / dark on sections; NO sequential locking; gamification post-launch. **Final mockups archived in `mockups/finalmockup/`** (nl-03 + landing-v8 + README with lock terms). **Phase 2 = build:** shell components + V2 tokens → topic routes (#37) → port Overview/section/reader → propagate to quiz/Test Centre/player/results → landing seam. No more mockup rounds.

> **🗺️ SITEMAP LOCKED (2026-07-26, decision #37 in plan §10) — three altitudes + topic routes:** (1) **Shelf** = All Chapters library OUTSIDE the shell (no sidebar; current BookHome role; V2 restyle in Phase 2; returning users land in last-open chapter, shelf one click up). (2) **Workspace** = chapter-scoped shell; pane depth capped at list→detail; section page is a child of Overview (crumb `Overview / Section N`, two segments max — chapter name lives in sidebar only). (3) **Immersion** = shell hidden, own minimal top bar, one exit: topic reader / section quiz / paper player. **Topics get URLs** `/chapter/:cid/section/:sid/topic/:tid` — today `activeSubsection` is component state in `SectionModule.tsx` (no deep links, broken back button; the v5 resume band requires the route). Guardrails: one destination one URL; immersion never links sideways; browser back = up one altitude. **Working demo: `mockups/nl-03-flow.html`** (hash-router, Overview → Section 2 → Topic 3 reader wired; render-verified desktop + 500px).

> **🎨 DESIGN LANGUAGE V2 "WORKSPACE" (2026-07-26, decision #36 in plan §10):** Neha wants a completely fresh app-interior language (landing keeps v8), seeded by her external-AI screenshots. **Canonical spec = `DESIGN_LANGUAGE_V2.md`** (three-column shell: grouped sidebar LEARN/PRACTICE/REVISE + main + rail; real-NCERT-art hero banners; tokens; the surviving laws — one-fact-once, honest completion, mock-tests-never-buried, no self-marking, no subscription-speak; §6 = iteration log of every adopt/refuse/park). Mockups (served via `cd mockups && python3 -m http.server 8877`): **`nl-02-chapter-home.html` v5** (merge of her AI's round: resume band, journey stepper w/ time estimates, profile block, light chip hero; mock-test rail CTA RESTORED after her AI deleted it; XP/badges/daily-goals PARKED as post-launch gamification roadmap — real feature, not paint) · **`nl-01-section-page.html` v4** (dark-cinematic hero variant; needs v5 shell propagation after the lock). **⛔ LOOP-EXIT RULE (Uday: "we are stuck in a loop"): no more external-AI mockup rounds. Neha answers THREE questions on v5 — (a) hero light-chips vs dark-cinematic, (b) sequential section locking yes/no (recommendation: NO locks, highlight recommended-next), (c) gamification launch vs post-launch — then the language LOCKS and Phase 2 BUILDS (shell components + port section page & chapter home, propagate to quiz/test-centre/player/results, landing seam last). Polish after lock = code tweaks, not mockups.** Render-verify every pass with headless Chrome (`--headless --screenshot`; NOTE: min window width ~500 — a 390 "overflow" was this artifact). Earlier same-day: A-fix #1 applied (quiz unit = completed>=1; `setSectionTotal` has NO callers — 5/5 was unreachable; A-fixes #2–4 PROPOSED not applied); Sprint-4-era review found section header uses topics-only math (inside/outside disagree — now absorbed into V2 port anyway).
> **⚠️ INFRA (2026-07-26): Supabase Site URL is TEMPORARILY `http://192.168.1.9:5173`** (Mac's DHCP IP moved .17→.9; plan-B again after a 500 from a scheme-less Site URL — it MUST include `http://`) → **REVERT to `https://historylab.in` at wrap-up.** Old `.17` redirect-list entries should be deleted (stale tabs get tokens delivered to a dead IP). **Recommend router DHCP reservation for this Mac** so links/auth stop breaking. Redirect-glob lesson stands: `/**` doesn't match the bare origin — list both forms.

> **🎨 UX REVAMP TRACK (2026-07-20, decisions #32–#35 in plan §10):** independent UX analysis accepted (test engine was buried in the tools drawer — Neha's core complaint). **Process locked (#35): page-by-page — mockup → deliberate → lock → rebuild.** Mockups in `mockups/revamp-0*.html`, served to Neha via `cd mockups && python3 -m http.server 8877` → http://192.168.1.17:8877/. **DONE: chapter home (#34) locked after 4 deliberation rounds and REBUILT live (`c3c6796`)** — numbered loop bands (1 Learn · 2 Test-band paid / Unlock-band preview · 3 tools w/ payloads), one-fact-once rule, ring labelled "% chapter done", bar only on in-progress sections, "MCQ marks" never "objective". **#32: self-marking REJECTED** ("the assessment is the reward"). **NEXT: revamp-02 Practice & Mock Tests deliberation** (also restores Exam Prep's home entry — currently /chapter/:cid/exam has NO button pointing at it), then 03 paper-start, 04 results, then the paper player itself. **Lesson: render-verify every UI pass with headless Chrome (`--headless --screenshot`) — mockup HTML lied twice (ring clipping, pill overlap).**

> **🔁 SPRINT-4 REVIEW ROUND 1 (2026-07-19, Neha live on the dev build) — decisions #27–#30 in plan §10 Round 9, ALL IMPLEMENTED (`6a02410`…`0cf0360`):** (27) profile = **Student + Teacher only, Parent dropped, class capture dropped**; (28) **purchase sheet simplified** (no parent handoff, no refund/GST line); (29) **progress = topics + quiz per section** (ring/bars; preview keeps whole-chapter denominator via `topicCount` in the get-chapter trim — redeployed); (30) **launch-0x mockups = the post-signin UI spec** — alignment pass for launch-06/07/08 SHIPPED (Test Centre hero/stats/attempts-table/featured card; player inline palette, grouped source sub-questions with (a)/(b)/(c), authorable **`HINT:` board-technique tips** (questions.hint migration + parser + function redeployed, seed v2 pushed), saved-ago indicator, submit card with blank-question jump; results score-hero ring, +N delta chip, section bars, written-answer cards, compact MCQ rows). **STILL PENDING from the alignment plan: launch-03 screen D (payment-pending/webhook-delay state) + signup/purchase restyle (apply #27/#28 over the mockup structure — mockups still show Parent role + handoff; decisions win).**
> **⚠️ OPEN GATE (decision #31, 2026-07-19): AI MARKING ON HOLD — Neha is revisiting whether written answers get AI-checked at all** (her instinct: RAG + local/small model since the domain is narrow; explore later with real papers — no architecture decided, schema blocks nothing). **Review model: Neha reviews only COMPLETE experiences — never present sprint-partial work.** Milestone plan: **A = complete student journey — DONE 2026-07-19 (`2737717`):** alignment passes 1+2 (launch-02/03/06/07/08) + payment-pending state + **Ch1 paper `c10-hist-ch1-p1` (17Q/29 marks, Frankfurt source study) + Ch2 sectional `c10-hist-ch2-p2` (11Q/20 marks/30 min) seeded+published** → **Neha single full E2E review** (exam-UX verdict + authoring-format verdict happen there) → **B** marking per her #31 call (₹149 examiner add-on rides with it — NOT missed, sequenced) → **C** launch hardening (live keys/legal/SEO/parent email/guardian verify/backups + prod deploy incl. the auth-deadlock fix).
> **⚠️ INFRA REMINDERS:** Supabase **Site URL is TEMPORARILY `http://192.168.1.17:5173`** (set 2026-07-19 to unblock Neha's OAuth on the dev server — the redirect allow-list match kept failing) → **REVERT to `https://historylab.in` after the review session.** Redirect-URL learning: a `/**` glob does NOT match the bare origin; keep both forms listed. Vercel testing project DELETED 2026-07-19 (testing = localhost only). The reload-logout auth-deadlock fix is still NOT on prod (`main`) — pilot students on historylab.in can hit stuck saves (Neha did); option offered to cherry-pick `1577d52` to main, Uday hasn't decided.

> **🚧 SPRINT 4 (test engine core) — BUILT + DEPLOYED 2026-07-18 (`714ca30`), EXIT TEST PENDING: Uday is walking Neha through it and has "a lot of feedback on the UI and UX" incoming — expect a feedback round before the sprint closes.** Shipped: migration `20260718100000` (papers/paper_sources/questions server-only — answer keys have NO client read path; attempts/answers read-own, **writes only via Edge Function** per §12.1) + seed migration `20260718110000` (generated by `scripts/export-paper.ts` from `content-src/papers/ch2-p1.txt`); **`test-engine` Edge Function DEPLOYED** (actions list/start/save/submit/result + admin upsert_paper/get_paper/set_status/delete_paper/admin_list; server owns the clock — deadline = start + duration + 30s grace, saves rejected past deadline, expired attempts auto-finalized on list/start; objective auto-marking on submit; one in_progress attempt per user+paper, unlimited re-attempts; anon→401 verified). Client: `src/lib/testEngine.ts` (typed wrapper, surfaces server error bodies), **Test Centre** `/chapter/:cid/tests` (paper cards + attempt history + best score; paid-only gate w/ PurchaseSheet CTA — server enforces the same via has_access), **PaperPlayer** `/tests/:paperId/play` (server-offset countdown, question palette + mark-for-review, 15s autosave heartbeat + flush on nav/tab-hide/unmount, MCQ + textarea w/ word count, submit confirm, auto-submit at 0:00), **AttemptResult** `/tests/result/:attemptId` (objective score, MCQ key reveal, per-point marking-scheme self-check until Sprint 5 AI marking), 🎯 tile on HomePage, **Admin → Papers tab** (paste Neha's structured text → `src/lib/paperFormat.ts` parser → line-numbered errors/warnings + preview → upload as draft → publish/unpublish/delete; re-upload warns it clears past answer sheets). **Authoring format for Neha: `PAPER_AUTHORING_GUIDE.md`** (plain-text: `Q. [marks]`, options a)–f) with `*` on the correct one, SCHEME:/MODEL: blocks, SOURCE…END SOURCE passages). **Seeded + published: `c10-hist-ch2-p1` "Nationalism in India — Practice Paper 1"** (17 Qs, 29 marks, 10 objective, 60 min, sections A–E incl. source-based). Build+eslint clean (router.tsx fast-refresh errors pre-exist). **Exit gate: Uday E2E attempt w/ objective marks + resume-on-refresh; Neha authors ≥1 paper via the guide. Then UI/UX feedback round (propose-before-implement).**
**ENVIRONMENTS: PRODUCTION = https://historylab.in** (+www) — Cloudflare Workers (project `historylab`, account Uday@teknomatics, builds `main` via Git; config = `wrangler.jsonc` static-assets + SPA fallback — the ONLY dev-era commit pushed to main, `67cad18`; build vars VITE_SUPABASE_URL/ANON_KEY + NODE_VERSION set in Cloudflare → Settings → Build). **TESTING = localhost (`npm run dev`, Node 20 via nvm) — the Vercel project (historylab-testing-kappa) was DELETED by Uday 2026-07-19; there is no hosted testing env.** All old vercel.app links are dead — students/pilot users must be given historylab.in. Auth whitelists DONE + sign-in on historylab.in VERIFIED by Uday (2026-07-12): Google origins + Supabase Site URL=historylab.in + redirect URLs (stale Vercel entries in Supabase/Google can be removed, harmless either way).

> **✅ SPRINT 3 CLOSED 2026-07-13 — full money loop proven with real test-mode gateway traffic.** Exit evidence: payment `pay_TErk7QhvOy2sRL` ₹199 netbanking captured → entitlement granted (first via manually-signed webhook injection when the browser handler died on redirect; sheet now self-heals `8c0d981`); dashboard refund `rfnd_TEsCgfBdpTxD86` (₹191 — **Razorpay withholds ~2% fees, refunds are PARTIAL; we absorb the fee under the 7-day policy**) → real `refund.processed` delivery → purchase refunded + chapter auto-revoked (Uday confirmed re-lock). **Webhook REGISTERED + API-verified** (active, both events). Working keys: `rzp_test_TErf7Eboldb6AT` (pact: NO more dashboard regenerations — three pairs died to that button; yesterday's "create-order bug" was a stale/expired key pair, root-caused via step-tagged errors `bdc92da`). ALSO fixed en route: **reload-logout auth deadlock** (supabase-js auth-lock held during onAuthStateChange while callback ran REST; all callback work now deferred, fallback restores persisted session — `1577d52`, predates this week, rides to prod at launch merge) + avatar broken-image fallback (`bb2744c`). Testing hygiene note: one account per browser profile (parallel sessions on one origin fight over the token and trigger reuse-revocation); incognito breaks Razorpay redirects + Google avatars.
> **⏭ NEXT: SPRINT 4 (test engine core — plan §9.1):** server-side `papers`/`questions` + `attempts`/`answers` schema (RLS: server-only writes for attempts/marks per §12), paper player (server timer, autosave, palette, submit), objective auto-marking, Test Centre + attempts history, **paper authoring format + admin upload so Neha starts authoring THIS sprint** (her pace = launch long pole). Exit: a full Ch2 paper attemptable E2E with objective marks; Neha has authored ≥1 paper.
> *(Resolved-bug archive:)* ~~create-order fails at runtime~~ Established facts: (1) FIRST key pair Uday supplied was invalid at Razorpay itself; he regenerated → **new keys `rzp_test_TCh15nXnXYSdDM` VERIFIED WORKING via direct curl to /v1/orders** (order created); (2) new keys set in Supabase secrets 18:38Z and **create-order + verify-payment force-redeployed AFTER that** (stale-env theory addressed) — still failing; (3) function itself boots (anon probe → 401), his auth works (get-chapter/S1-preview work on same localhost session), so the failure is a 4xx/5xx INSIDE create-order after auth. **NEXT SESSION, in order: (a) read Supabase dashboard → Edge Functions → create-order → Logs for the newest invocation (my code console.errors the Razorpay response body on 502 and any purchase-insert error path returns 500) — Uday can paste it; or (b) faster without Uday: temporarily instrument create-order to return a step tag in the error JSON (product-lookup / entitlement-check / purchase-insert / razorpay / profile), redeploy, one click, read, revert.** Suspect ranking: purchases INSERT failing (check constraint/column mismatch?) > Razorpay reject despite valid keys (unlikely — same call works via curl) > products row shape. NOTE: client shows the same generic message for ANY non-409, so the message itself carries no signal. Webhook dashboard registration ALSO still pending (URL + secret in HANDOFF below).
> **SPRINT 3 (money, test mode) — CODE SHIPPED + DEPLOYED 2026-07-12 (`a9a9c03`), E2E PENDING the bug above.** Edge Functions live: `create-order` (server-priced from products, double-purchase 409, purchases row = receipt, returns key_id so NO client env var), `verify-payment` (HMAC handshake → paid + entitlement upsert; order must belong to caller), `razorpay-webhook` (**deployed --no-verify-jwt**; raw-body HMAC vs RAZORPAY_WEBHOOK_SECRET; idempotent on x-razorpay-event-id via payment_events unique; captured→grant, refund.processed→revoke). Client: `src/lib/razorpay.ts` + PurchaseSheet "Pay now" (success→reload, failure/dismiss states). Secrets in Supabase function env (test keys; webhook secret generated — value in Razorpay dashboard + Supabase secrets, NOT in repo). Probed: anon 401, bad sig 400, valid sig 200, duplicate detected. **PENDING (Uday): (1) Razorpay dashboard → Webhooks → add `https://zqryzlkvieljadntlomz.supabase.co/functions/v1/razorpay-webhook` with events payment.captured + refund.processed and the generated secret; (2) test-card E2E on the dev build; (3) exit test incl. refund→revoke from Razorpay dashboard.** Live keys swap = `supabase secrets set` at launch (Sprint 6).
> *(Sprint 0 history below:)* **SPRINT 0 (cross-device sync) — CODE COMPLETE, MIGRATION PENDING APPLY.** Built: `supabase/migrations/20260712235900_sprint0_progress_sync.sql` (student_progress blob→fine-grained rows kind∈{meta,section,subsection,quiz,legacy}, flashcard_state sync cols + delete policies, **namespaced chapter ids `c10-hist-ch1`** — helpers in `src/lib/contentIds.ts`); sync engine `src/lib/progressSync.ts` (hydrate at sign-in via AuthGuard → **keep-highest merge** `src/lib/progressMerge.ts` (28 unit tests pass, pure/no-IO) → debounced flush of persisted dirty queue `src/lib/syncBus.ts`, offline retry, shared-device owner guard: first sign-in claims local pilot data = the localStorage migration; different account → local reset); **progress store now chapter-keyed** (`chapters[cid][sid]` — fixes pre-existing ch1/ch2 conflation; persist v1 + migrate from v0; call sites updated: HomePage, SectionModule, Quiz/Timeline/Exam/Figure/Map/NarrativeMode); flashcards get `lastReviewedAt` (LWW sync). Old write-only `syncProgress.ts` deleted. tsc + build + eslint clean (new files).
> **✅ MIGRATION APPLIED 2026-07-12** — CLI linked to project `zqryzlkvieljadntlomz` (name "historylab", ap-northeast-1), `supabase db push` succeeded, `migration list` shows `20260712235900` local=remote, new columns verified via REST probes (200 on kind/item_id/updated_at selects). **No schema baseline migration exists** (`db pull` needs Docker, not installed — migration history simply starts at Sprint 0; pre-existing schema documented in the root `supabase-*.sql` files). Supabase mode = CLI (`supabase login` done by Uday).
> **✅ SPRINT 0 EXIT TEST PASSED 2026-07-12** (Uday: fresh account `teknomaticspm@gmail.com` on his Mac — owner guard correctly reset the other account's local state — then incognito sign-in with the same account hydrated flashcard state from the server). **SPRINT 0 CLOSED → SPRINT 1 CLOSED 2026-07-12 (exit test passed: grant+revoke from /admin worked) → SPRINT 2 PAYWALL CORE DONE; REMAINDER ⏸ PAUSED ON A MODEL DECISION.**
> **✅ DECIDED + SHIPPED same day (plan §10 decision #26): free tier = Ch1 SECTION 1 only, in-chapter preview; Ch1 now ₹199/₹499 paid; NO grandfathering of pilot accounts.** Implemented end-to-end: `preview_section` on products + `has_access()` admin bypass (migration `20260713030000`), **BOTH chapters exported server-side** (`2026071303010*_content.sql`; export script now emits both + sourceAnalysis/imageAnalysis in the bundle shape), `get-chapter` deployed with a server-side S1-trim branch (paid content never reaches an unentitled client), client fully cache-based (`getChapter.ts` has ZERO static chapter imports — bundle 1120→862KB, eviction grep-verified for both chapters), map cards chapter-aware (fixed latent ch1-only `getMapById`), preview UI: HomePage locked section cards → PurchaseSheet, SectionModule deep-link bounce, BookHome "First section free" chip, `evictPreviewBundles()` on entitlement refresh. **Landing copy rule: "first section free", never "Chapter 1 free".**
> **✅ PUBLIC FACE SHIPPED (2026-07-12):** signed-out `/` now renders the **landing page** (faithful React port of frozen `mockups/landing-v8.html` → `src/modules/landing/`, copy updated per decision #26; prices LIVE from products table so admin Pricing-tab edits flow to the landing; class-interest + parent-newsletter captures write to their tables, dup-tolerant; all CTAs → Google sign-in — LoginPage retired from the flow). **Legal pages** `/terms` `/privacy` `/refunds` live signed-out AND signed-in (DRAFT wording — **Uday must review before Razorpay onboarding**). Verified via headless-Chrome screenshots of the built app (all bands + dynamic ₹ render). **Admin Pricing tab** added (`/admin` → Pricing) — Neha can reprice without SQL. **Sprint 2 remaining:** domain historylab.in + Cloudflare Pages migration (EXTERNAL GATE: Uday DNS + Cloudflare account), OG/meta/SEO polish (Sprint 6), real testimonial quotes (Sprint 6). Then Sprint 3 (Razorpay test mode) — gated on Teknomatics KYC.
> **Sprint 2 core SHIPPED (2026-07-12):** `chapter_content` table (NO client RLS — service-role only) + generated content migration (`scripts/export-chapter-content.ts` → 266KB ch2 bundle, regenerate+push on content change); **`get-chapter` Edge Function DEPLOYED** (JWT → `has_access()` → content; anon=401 verified); client: **ch2 imports fully evicted from the JS bundle** (verified: Chauri Chaura/ch2-fc-*/Champaran absent from dist; only public book metadata remains), `src/data/chapterBundle.ts` session cache + `getChapter.ts` accessors read it, RequireChapterAccess awaits bundle with loading state, BookHome 3-state cards (open / **locked = warm price chip ₹199+struck ₹499 → PurchaseSheet** / coming-soon), PurchaseSheet with WhatsApp+email **ask-your-parent handoff** (progress summary + honest "payment opens soon"). Remaining Sprint 2 after decision: landing v8 → React route + captures + ToS/Privacy/Refund + domain/Cloudflare Pages (EXTERNAL GATE: Uday DNS).
> Sprint 1 detail (for reference): Migration `20260713010000_sprint1_access_admin.sql` APPLIED: `admins` table (no client policies) + `is_admin()` (seeded: udaysharma10@gmail, uday@teknomatics, nehaudaysharma@gmail — only where profiles existed; promote later via SQL `insert into admins select id from profiles where email='…'`), **teacher-role RLS policies DROPPED (privilege hole: any user could self-set role='teacher' and read same-school data)**, products (c10-hist-ch1 free / c10-hist-ch2 ₹199-launch-₹499-list / examiner-review ₹149) + purchases + entitlements (admin insert `admin_grant`/delete; purchase rows = webhook-only) + `has_access()` + examiner_reviews + payment_events + class_interest + parent_updates, guardian_email/guardian_consent_at on profiles, **Round-8 allowlist replaced by seeded team grants** (`chapterAccess.ts` deleted). Client: `AccessProvider` (products+entitlements+is_admin rpc → `canAccessChapter`, mounted in AuthGuard), ProfileSetup reworked (student/parent only, guardian email required for students + timestamped DPDPA consent), **AdminPanel at `/admin`** (users w/ entitlement chips, grant+note/revoke, purchases tab), AppShell Admin+Dashboard buttons server-driven, BookHome admin auto-redirect to /dashboard REMOVED (Neha uses the app). Verified: anon RLS probes (products public, entitlements/admins empty, is_admin false), tsc/build/dev-server clean. **EXIT TEST (Uday): sign in as udaysharma10 → Admin button → /admin → grant ch2 to teknomaticspm@gmail.com → that account sees Ch2 like Ch1 (revoke → back to Coming Soon).** The LIVE app is unaffected by the Sprint 0 migration (its old blob upsert now fails silently; it was write-only and never read). External gates: Teknomatics Razorpay KYC (gates Sprint 3 live keys), historylab.in DNS→Cloudflare (Sprint 2), Neha paper authoring (Sprint 4+), 2 real student quotes (Sprint 6).
> *(Context: mockup gate closed 2026-07-12; execution order = **PLAN_SUBSCRIPTION_LAUNCH.md §9.1 Sprint plan v2**; architecture/security/cost = plan §1–§2, §12, §13. Landing FROZEN at `mockups/landing-v8.html`.)*
> **Repo state:** all monetisation work is LOCAL on `dev` (commits `a0fd25d`…`817c719`, not pushed; origin/main still `b160636` Round 9 — live app untouched). Decision log rounds 1–7 = plan §10 (25 locked decisions). Mockups: `launch-00..10` (annotated) + `clean/` + `landing-v4..v8` + assets.
>
> *(Superseded pre-freeze checklist below, kept for context:)*
> **⏭ NEXT SESSION (Uday: "we will come to this tomorrow") — the monetisation/landing track is waiting on:**
> 1. **Review `mockups/landing-v8.html` (CURRENT — merge of v7 + ChatGPT concept, 2026-07-12)**: "History, finally understood." hero + iPad device frame (real screen), illustrated problem trio (3 ILLUSTRATION SLOTS awaiting ChatGPT assets), "From story to score in 4 steps" journey w/ progress graph, anchor nav, rich footer + parent newsletter. Examiner band/roadmap/pricing preserved. Superseded: v7 pain band + reasons band + how-it-works band (merged into journey). Previously: **Review `mockups/landing-v7.html`** — v6 restructured per external marketing feedback (2026-07-12): pain→pivot band ("Sound familiar?" / "History isn't difficult, it's taught the wrong way"), "Three reasons families choose" band, all walkthrough copy cut ~40%, "But there are free videos on YouTube…" comparison table, 6-tool strip. Examiner band + roadmap PRESERVED (locked decisions). **Header/theme variant pick still open:** v6 white vs `landing-v6-warm` vs `landing-v6-adaptive` — v7 is on the white base; the winning treatment applies to v7 in minutes.
> 2. **Their scrolled content feedback on the landing page** (they collected it, not yet shared).
> 3. **2 real pilot-student quotes** from Neha → replace the marked "sample quotes" in the ink testimonial band.
> 4. After landing sign-off → **mockup gate closes → start Sprint 0** (server-side progress + SM-2 sync; sprint pipeline in PLAN_SUBSCRIPTION_LAUNCH.md §9).
> 5. Parallel (Uday, calendar-critical): **Teknomatics Razorpay onboarding + GST**; domain historylab.in wiring (owned, not wired).
> Read `PLAN_SUBSCRIPTION_LAUNCH.md` §0 + §10 (decision log rounds 1–6) before touching anything — pricing/access/examiner decisions are all there.
**Repo:** `github.com/udaysharma10/historylab` · **Local:** `/Users/udaysharma/Documents/Blostem/Claude_Projects/vedansh-history`

> **Round 9 (2026-07-01) — MCQ "all options showing wrong" bug FIXED (commit `5fae261`, on `dev`).** Neha's student: *"in few MCQs all the options are showing wrong"* — in the **Exam Prep** section. Root cause: `SourceQuizCard` (Source Comprehension quiz) and `ImageAnalysisCard` (figure practice) had **no stored correct answer** — they inferred it by substring-matching the model-answer text against each option (`options.findIndex(opt => answer.toLowerCase().includes(opt.toLowerCase()))`). Whenever the answer didn't contain an option *verbatim*, `findIndex` returned **-1**, so no option was ever green and every pick showed red. Measured impact: **all 3/3** source-comprehension MCQs and **7/10** figure MCQs were broken. **Fix:** added an optional authoritative `correctIndex` to the `SourceComprehensionActivity` + `ImageAnalysisActivity` question types; both cards now prefer `correctIndex` and only fall back to substring-matching for legacy data; populated `correctIndex` on every affected MCQ (`data/ch1/activities/sourceAnalysis.ts`, `imageAnalysis.ts`). Verified via typecheck, Node-20 `npm run build`, and a resolver simulation (`/tmp/verify_mcq.mjs` — recreate; confirms old method returned -1 where new resolver returns the right option). **Lesson: never infer a quiz's correct answer from prose — store it explicitly.** Note: the graded `MCQCard`/`InlineQuizCard` (section quizzes) were always fine — they use a real `correctIndex`. **DEPLOYED 2026-07-02 (`b160636` on main).**

> **Round 8 (2026-07-01) — Ch2 figure HOTSPOT plotting reviewed & fixed (Neha flagged dots "not in place").** Now that Ch2 figures render, Neha saw the interactive hotspot dots were mis-placed. Re-verified all 16 Ch2 figures at full res (recreate `scratchpad/annotate_ch2.py` — parses `figures.ts`, overlays a 10% grid + current dots on each image; needs PIL). **Two problem classes fixed in `src/data/ch2/figures.ts`:** (1) **Unverifiable person-IDs on grainy photos** — `fig-6` (Allahabad group) named Gandhi/Patel/Nehru/Bose at dots landing in gaps between seated/turned-away figures → **de-identified** to one honest "Congress leaders in discussion" hotspot (analysis softened too); `fig-2` "Gandhi leading" pinned an indistinct speck → reframed to "Head of the march". (2) **Dots drifting off the object** — `fig-14a` trishul floated in the sky → moved onto the trident tip; `fig-10` Gandhi/Nehru/Azad were on chests → raised to faces; `fig-12` had a vague "four arms"/"learning-food-clothing" bundle → replaced with **5 precise dots on the face + the 4 held items** (white cloth, book, mala, paddy); `fig-11` Tilak on torso → face; `fig-7` Gandhi low/right → onto the bald staff-figure; `fig-13`/`fig-9`/`fig-1` nudged onto their targets. Already-correct (left as-is): fig-3, fig-15, fig-4, fig-5, fig-8, fig-14b, fig-16 (no hotspots). **Verified in the running app** (fig-12 & fig-14a detail screenshots — pins land on the items). **Teacher principle applied: prefer a general, defensible hotspot over a precise-but-wrong "this is X" that misteaches.** Hotspot method note: `{x,y}` = % of displayed image; tooltip flips above when `y >= 58`.

> **Round 8 — Chapter-access gate for the student feedback trial (email allowlist).** Neha is sharing the link with students for Ch1 feedback and wants **only Ch1 visible to them; Ch2 (and the rest) disabled**, while the team keeps Ch2 access. Implemented a **client-side gate** (`src/lib/chapterAccess.ts`): Ch1 is `isFree` → open to all; gated chapters need an unlock email. **Unlock allowlist** (case-insensitive, edit `CHAPTER_UNLOCK_EMAILS`): `udaysharma10@gmail.com`, `uday@teknomatics.com` (also admin), `nehaudaysharma@gmail.com`, `neha.sharma-socials@ggn.hxls.org`. `BookHome` shows a locked chapter exactly like the not-built ones ("Coming Soon", greyed, non-clickable); a `RequireChapterAccess` layout-route guard in `router.tsx` redirects direct `/chapter/ch2/*` deep-links to `/` for non-allowlisted users. **Verified in running app** (student sees Ch2 "Coming Soon" + deep-link bounces to home; allowlisted sees Ch2 clickable). **Caveat:** UI gate only — content still ships in the bundle (a determined user could read Ch2 from dev-tools); fine for a student trial. A true server-enforced lock = `PLAN_SUBSCRIPTION_LAUNCH`.

> **Round 7 also fixed a navigation bug + ran a Ch2 teacher review (see below the main callout).**

> **Round 7 (2026-06-30) — Ch2 study tools were authored but never wired up.** Neha approved Ch2 *content* but flagged "not a single NCERT picture, and no map work (2-mark board Q)." Root cause: the app was born single-chapter; the mode components (`NarrativeCard`/`FigureMode`/`FigureGallery`/`TimelineMode`/`TimelineExplorer`/`ExamPractice`/`MapMode`) were never migrated off the hardwired **ch1-only** static arrays (`data/figures`, `data/keyDates`, `data/sources`, `data/activities/*`, `data/maps`) onto the chapter-aware `getChapter.ts` accessors — and the Study-Tools tiles were gated `chapters:['ch1']`. So Ch2 showed only Flashcards, and any Ch2 `imageId` resolved to `undefined` (rendered nothing). **DONE & verified in running app:** Ch2 now has **Figures, Timeline, Exam Prep** (+ Flashcards) — figures render inline + in the gallery; Timeline browses 20 key dates (1915–1942); Exam Prep has 5 primary sources + 8 NCERT board questions. Sub-features lacking ch2 activities are hidden gracefully (figure image-analysis practice, timeline ordering practice, source-comprehension quiz). **DONE — Maps (Piece B):** Ch2 now has all 5 Study-Tools tiles. Built `src/data/ch2/maps.ts` ("Centres of the Indian National Movement") + `ch2/activities/mapActivities.ts` (5 identify + 3 label); made `MapMode` + `MapExplorer` chapter-aware (`getMapDefinitions`/`getMapIdentify/LabelActivities`); un-gated the Maps tile. **India base map rendered from public-domain Natural Earth data** (`scratchpad/render_india.py` — recreate; needs `pyshp`+`matplotlib`+the ne_50m admin_0/admin_1 shapefiles); each city's x/y% is computed from its real lat/long, so pins are exact (no grid guessing). Map shows the 9 CBSE board-map places (Champaran, Kheda, Ahmedabad, Amritsar, Chauri Chaura, Dandi + Congress sessions Calcutta/Nagpur/Lahore). Explore mode is pins-only + reveal-on-tap (clustered Gujarat/Punjab cities made all-labels-at-once unreadable). Verified in running app. Image: `public/images/ch2/india-national-movement-map.png` (1115×1430). Process lesson: Ch2 figure QA was done at the *data/coordinate* layer (PIL grid on image files), never in the *rendered chapter* — a correct-coords image that never renders looks identical when you only inspect data. **Always verify each chapter in the running app, not just the data.**

> **Round 7 — "Next Topic" navigation bug (FIXED, commit `8694462`):** after finishing a topic, the SubsectionComplete "Next Topic →" button didn't open the next topic — `SectionModule` rendered `<NarrativeMode>` **without a key**, so React reused the instance and its internal state (`showComplete=true`, `cardIndex` at end) persisted across the subsection change → the next topic opened straight onto another "complete" screen. Fix: `key={subsection.id}` on `<NarrativeMode>` (forces a fresh remount per topic). Pre-existing, shared code — affected both chapters.

> **Round 7 — Ch2 teacher review (Claude, history-teacher hat) — content is strong, figures under-integrated.** Read all 4 Ch2 sections: NCERT-faithful, accurate (no factual errors found), strong pedagogy (WHY cards, flowcharts, the differing-strands table, exam-prep cards, cross-chapter synthesis). **Figures-as-learning gaps (OPEN — pending Uday/Neha go-ahead before editing content):**
> - **Section 2 ("Differing Strands") has ZERO inline figures** — yet it's the most exam-heavy section, and two NCERT figures tagged to it sit **unused in the gallery**: **`ch2-fig-4` "Boycott of Foreign Cloth"** (→ the towns/boycott cards) and **`ch2-fig-5` "Chauri Chaura"** (→ the Chauri Chaura cards).
> - **Simon Commission card** (`section3-civil-disobedience.ts` ~L41, 1928) uses **`ch2-fig-6` "Meeting of Congress Leaders at Allahabad, *1931*"** — an unrelated, later photo (fig-6 is also already flagged `NEEDS NEHA`). Swap or drop.
> - **`ch2-fig-2` "Indian Workers in South Africa March" (s1)** is gallery-only — would fit the satyagraha-origins cards.
> - Cosmetic: `chapter2.ts` S1 `figureIds` is out of sync with the figures actually used inline (lists fig-2/fig-4; omits fig-15/fig-16 Jallianwala/Dyer). Rendering uses card `imageId`, so this is metadata only.
> - Integration density: Ch1 = 19/21 figures inline; **Ch2 = 13/17** (S1:4, **S2:0**, S3:4, S4:5).

This is a Grade-10 CBSE History self-learning web app (NCERT "India and the Contemporary World"). Ch1 = Rise of Nationalism in Europe, Ch2 = Nationalism in India. See `PROJECT_CONTEXT.md` for full architecture, `CONTENT_REVIEW_PROCESS.md` for the review log.

**Reviewers:** Neha (CBSE History teacher — content accuracy + pedagogy) and Uday (product/UX). Working model: **section by section, two hats — (1) history/NCERT accuracy, (2) self-learning experience design** (no teacher in the room, so every card must carry its own scaffolding).

---

## THREE TRACKS

### 1. CONTENT TRACK (active — with Neha) — fixing content issues first, before theme/UX
Neha reviews **one section at a time**; we fix, deploy, she re-reviews.

**Status:** Neha has reviewed **Ch1 S1** + flagged a flowchart bug. Ch1 figure hotspots fully re-verified.
- ✅ Tightened S1 intro **13 → 9 cards**; fixed Sorrieu (fig-1) hotspots + tooltip-clipping bug.
- ✅ **ALL Ch1 figure hotspots re-verified at full resolution** (Round 4, commit `3c31ff6`). Notable fixes: fig-13 Bismarck/deputies were **swapped**; fig-20 Britannia was mid-ocean → **bottom-centre**; fig-10 women's gallery → **upper-left**; fig-4 German inscription → **white placard under the Tree of Liberty** (confirmed, no longer a guess); plus figs 3,7,8,11,12,15,17,18,19. Method: full-res PIL grid overlay (`scratchpad/annotate.py` — recreate, doesn't persist).
- ✅ **Fixed a systemic rendering bug** (commit `c2090b3`): a card with BOTH a rich body (flowchart/table/figure) AND an `inlineQuiz` showed ONLY the quiz — the steps/table/image were never rendered (this was Neha's "tap each step below — where?" bug). `NarrativeMode` now renders the full card body AND the quiz beneath it for non-text cards; `InlineQuizCard` hides its text block when empty. Also merged the two stranded intro text cards into their flowchart/table cards (S1 French measures, S2 Habsburg).

**Round 7 — Ch2 figures now render (Piece A, DONE):** Made figure rendering chapter-aware end-to-end. `getFigures(chapterId)` (returns `Figure[]`) is now used by `NarrativeCard`'s `FigurePanel` (chapterId threaded from `NarrativeMode` → `NarrativeCard`), `FigureMode`, and `FigureGallery` (new `chapterId` prop). Added `'print'` to `FigureType` and made `Figure.caption` optional so `Ch2Figure` is assignable to `Figure`; `FigureDetail` guards the now-optional caption. Added `CHAPTER_SECTION_LABELS` to `getChapter.ts`; `FigureGallery` derives its type-filter chips from the figures actually present. Un-gated the **Figures** Study-Tools tile for ch2 (`HomePage.tsx`). Ch2 has no image-analysis practice activities yet, so `FigureMode` hides the "Practice: Image Analysis" card for ch2 (`hasPractice`). **Verified in running app:** Ch2 Figures tile shows; `/chapter/ch2/figures` lists all 17 ch2 figures; gallery renders the real NCERT thumbnails (Mass Processions, Dandi March, Bharat Mata, Jallianwala Bagh…). Same `getFigures`+`imagePath` path the narrative cards use. **Still TODO (optional polish):** author ch2 image-analysis practice activities so the Practice card returns for ch2.

**Round 7 — Ch2 MAP WORK (Piece B, OPEN — board 2-mark question):** Ch2 still has NO `maps.ts`, no India map image, no map activities. `getMapDefinitions('ch2')`/`getMapsBySection('ch2')` return `[]`; the **Maps** tile is gated `chapters:['ch1']` in `HomePage.tsx`; `MapMode` is still hardwired to ch1 `data/maps` + `data/activities/mapActivities`. To do: source an India political map image (deferred decision — public-domain/Wikimedia vs NCERT extract), build `src/data/ch2/maps.ts` with board-relevant locations (Champaran, Kheda, Ahmedabad, Amritsar/Jallianwala, Chauri Chaura, Dandi, Nagpur 1920, Lahore 1929, Bardoli, Calcutta, Madras…), wire `getMapDefinitions`, un-gate the Maps tile, make `MapMode` chapter-aware (mirror the FigureMode pattern), author identify/label practice. Coordinates need full-res visual verification like the figure hotspots.

**OPEN — content track:**
- ✅ **ALL Ch2 figure hotspots re-verified at full resolution** (commit `f976c07`). Fixes: fig-7 Gandhi was on the wrong marcher; fig-10 Gandhi/Nehru/Azad all shifted (Gandhi's dot was on Nehru); fig-12 Bharat Mata symbols moved onto the actual hands/objects; fig-14a lion was on her dress; fig-13 onto the held image; fig-11 unity symbols → temple roundel; fig-3 soldier onto the soldier. (Ch2 field order: `label, description, x, y`.)
- **fig-6 (Allahabad Congress leaders) FLAGGED `NEEDS NEHA`** in code: grainy group photo, individual identities (Patel/Nehru/Bose) can't be verified from the image and current coords contradict the "extreme left/right" caption. Left unchanged pending Neha's authoritative key (don't guess — risk of mislabelling leaders).
- ✅ **Ch2 MCQ answer positions rebalanced** (commit `5f97cbd`) — inline quizzes + Practice-Quiz bank were "always B" (40/42 at option B); now 11 A / 10 B / 10 C / 11 D (options reordered, answer text preserved). Ch1 was already balanced. **Rebalance method:** `scratchpad/rebalance.py` (swaps two option literals + updates `correctIndex`; doesn't persist — recreate if needed). If you ADD Ch2 MCQs later, re-check the distribution.
- ✅ **Neha S1 round 2** (commit `e0e2136`): removed the **fig-1 "United States & Switzerland" hotspot** (not in NCERT — only that dot, nothing else); fixed the **Europe-1815 interactive map** dots that sat on water (Prussia, Spain, Ottoman) → moved their region centres onto land in `src/data/ch1/maps.ts`. NOTE: map dots = **centre of the `{x,y,width,height}` box** (`x+w/2, y+h/2`), `InteractiveMap.tsx`. The fig-3 *figure* Ottoman hotspot is at a similar low spot — nudge it too if Neha asks (left for now).
- Neha has **not** reviewed S2–S6 (Ch1) or any Ch2 content card-by-card yet. Ch1 S1 is the only section reviewed; she is moving forward to broader review now.
- The `FigureHotspotOverlay.tsx` tooltip flips **above** the dot when `spot.y >= 58` and clamps horizontally (so dots up to ~92 are fine).

**Neha's structural rulings — DO NOT RELITIGATE:**
1. Keep **flowcharts** even for parallel-list content (students grasp/remember them better).
2. Keep **tap-to-reveal + per-card highlights** (do not design down to passive students).
3. Tighten Sorrieu intro — ✅ done.

### 2. UI THEME TRACK (✅ DONE — "Cool Peach & Lavender" C2 live, commit `8edc6f7`)
Fraunces (display) + Inter (body) via `@fontsource-variable/*`. Current palette = **C2 (peach-led)**; the earlier navy "mature" theme was replaced after Uday found it too dark.
- **Theme tokens** (`src/styles/globals.css` `@theme`): deep plum ink `#3E3548`, **peach lead accent `#DC835F`** (brand "Lab", CTAs, ring), dusty lavender secondary `#7E72C2`, **peach→lavender bg `#FBEFE7→#F1ECFA`**, muted jewel section colours (`#C36B53 #5571B5 #C2893E #5C9368 #9B5C9A #3F8E84`), plum-tinted shadows; stars kept warm gold `#C99A3A`.
- **Section badges are SOFT TINTED chips** (`color+'1A'` wash, not solid fills) — the key "less heavy" move. HomePage has no top accent bar; ring is peach→lavender.
- All hardcoded old hexes migrated app-wide, including the **duplicated per-mode `SECTION_COLORS` maps** (figures/sources/flashcards/timeline/maps/exam) + `CHAPTER_SECTION_COLORS`. If you re-theme again, update those too.
- Mockup exploration in `mockups/home-refined*.html` (A warm-cream, B cool-gallery, C/C1/C2/C3 lavender-peach). **C2 is the shipped one.**
- Fixed: the breadcrumb was overlapping the hero (removed a negative margin).
- **Known pre-existing issue (NOT from theme work):** horizontal overflow at ~390px phone width. App is iPad-first; separate mobile-IA task.

### 3. UX / IA TRACK (PARKED — resume after content)
- ✅ Done: "Learning Modes" → **"Study Tools"**; section "Start Reading" top CTA; **Study Tools confirmed showing all 5 tiles** on Ch1.
- ✅ **Breadcrumb navigation** (commit `f4cc6d5`): the header back arrow is `navigate(-1)` (history-based), so Section→Quiz→back stranded Neha with no way to the chapter menu. Added breadcrumbs (`All Chapters · Chapter N · Section [· Quiz]`, with Chapter/Section as links) on **SectionModule** + **QuizMode**. Also fixed QuizMode "back to section" to the chapter-aware route `/chapter/:cid/section/:sid` (was hitting the `/section/:sid` redirect). **TODO if wanted:** extend the same breadcrumb to the 5 study-tool screens (Timeline/Maps/Figures/Flashcards/Exam) for full consistency.
- ✅ **Flashcard fixes** (`bfd3ff3` + `c09fe08`): (1) rating buttons were clipped on long answers — the flip card's two faces are `absolute inset-0` so they didn't grow a fixed-height container; `FlashcardSingle` now measures both faces (ResizeObserver) and sizes to the taller. (2) Card blended into the bg — added `border border-hist-line` + `shadow-card-hover`. **Flashcard modality = SM-2 spaced repetition** (`engine/spacedRepetition.ts`, persisted to localStorage `vedansh-history-flashcards`); "Review Due" is schedule-driven, "Practice All" = random 20, "Practice by Section" = filtered. Uday confirmed: **keep the modality as-is.**
- **Open:** "Topics" list still mixes "Practice Quiz" in with lesson topics; **mobile (~390px) horizontal overflow** (pre-existing, see Theme track); full screen-by-screen IA pass.

### PRODUCT / MONETISATION (model REVISED 2026-07-07 + positioning 2026-07-11 — landing at v6, code not started)
- **Landing evolution (2026-07-11):** v4 (card layout, screenshot-review found it unshippable) → **v5 (band grammar: hero w/ marking demo, legible product crops, ink testimonial band)** → **v6 (CURRENT for review): examiner elevated as THE differentiator** — hero chip + credential line, dedicated "red pen" band ("A Senior CBSE Examiner stands behind every mark", red-pen marked-card visual), 3rd pricing card "Examiner's personal review ₹149/paper (Add-on)". **Neha's rules: never named/shown ("Senior CBSE Examiner, 20 yrs" only); NO public capacity/slot messaging** (slot counter removed from launch-08 too). Header/theme clash (cream header vs lavender band) → three variants for Uday+Neha to pick: `landing-v6.html` (white header) / `landing-v6-warm.html` (all-warm) / `landing-v6-adaptive.html` (transparent→white on scroll, small JS). Real-app crops in `mockups/assets/shot-*.png` (recreate via dev-server + devpreview bypass + PIL crop; AuthGuard restored after). Sample testimonial quotes still pending real pilot quotes.
- **Round 5 (2026-07-11, Neha's positioning):** brand = **"the home of school History"** (subject-level promise, all classes eventually — her vision); offer stays concrete ("Live now: CBSE Class 10"); NO class number in the hero H1. Landing gained a **classes roadmap section** (Class 10 live / Class 9 next / 6–8 coming) + per-class "notify me" email capture (`class_interest` table — demand list, not a gate). **Expansion is level-first (History across classes), superseding BIGGER_PICTURE Phase E subject-first while brand = historylab.in.** Neha OK'd inner pages "for now" — her final validation happens on real built pages (Vercel preview in S1/S2), decision #19.
- **Round 4 (2026-07-07, Neha's review — SUPERSEDES invite-only/₹499):** (a) **OPEN ACCESS** — no invite gate/waitlist/codes at all (all-India CBSE TAM over metro exclusivity); (b) **₹199 launch price, ₹499 list** (struck through); (c) **test engine is PAID-only** — free Ch1 = learning modes; landing shows a static sample marked answer; (d) **NEW: two-tier marking** — AI marking included with chapter, **human "CBSE Examiner" marking = ₹149/paper add-on (price TBD)**: Neha marks from AI pre-marking in an admin queue (~5 min/paper, 48–72h SLA, weekly slot cap ~25–40, `examiner_reviews` table); (e) product-review fixes #1 (required guardian email at signup) + #2 ("Ask your parent to unlock" WhatsApp handoff) are launch requirements. Mockups v2: 01+02 rewritten (open funnel, marking-sample hero, guardian email); 03/04/06/08 updated (₹199, no invites → examiner orders, paid-tests framing, examiner upsell w/ slot counter). Plan doc §0/§5/§7/§9/§10/§11 rewritten.
- **Launch decisions round 2 (2026-07-02):** merchant of record = **Teknomatics** (NOT Blostem) for Razorpay + GST; launch choreography = **admin comps chapters FOC to the pilot cohort** (no direct selling to students; parents buy after viral spread); `historylab.in` already purchased (Neha). **Execution gate: Uday validates mockups before any code.**
- **Launch decisions round 3 (2026-07-02):** (a) **public launch only WITH the CBSE test engine** — S1 payments/paywall built + verified in test mode but not public alone; sprint order S0→S1→test engine→launch→trust/ops; (b) **mockup gate covers ALL sprints** — every screen through Sprint 6 validated before code. New mockups `launch-06..10`: test centre, paper player (server timer, 4 CBSE Q-types, autosave), AI-marked results (per-point ✓/✗ vs marking scheme, marker's notes, re-check), parent weekly email, email-auth + 2-device limits + S0 sync states.
- **Mockups DONE (`mockups/launch-0{0-5}*.html` + `launch-0{6-9}/10*.html`, open `launch-00-index.html`):** landing+waitlist, invite signup (code→Google→DPDPA consent), locked-Ch2→purchase→success/pending, account+3 invites, admin panel (users/grants/invite-batches/waitlist/purchases). All in the C2 design language; dashed boxes = spec annotations tied to PLAN_SUBSCRIPTION_LAUNCH.md. Verified via headless-Chrome screenshots. Key design intents: locked premium card looks desirable (warm, not greyed); admin grants render identically to purchases for the student; no teacher role anywhere.
- **`PLAN_SUBSCRIPTION_LAUNCH.md` (rewritten 2026-07-02) is canonical.** Brainstorm with Uday closed: **premium invite-only founding cohort** (Class 10 only for now; 3 invites/student + admin batches + waitlist landing), **₹499 one-time per chapter** (lifetime, NO subscriptions — Razorpay one-time Orders only), **no teacher role** (admin creates accounts / grants-revokes chapter entitlements from backend for anyone, FOC comps), anti-sharing = **personalised study state + CBSE-pattern practice tests with real marking schemes** (S2 moat). BIGGER_PICTURE's freemium/₹499-yr tiers are superseded (note added there).
- **Key insight unchanged:** content ships in the JS bundle, so the paywall must **move premium chapters server-side** (Edge Function + `entitlements` check); UI gate is cosmetic. Don't-skip: DPDPA parental consent (parent-pays model provides it), GST invoices, ToS/Privacy/Refund pages before Razorpay go-live. Sequence: S0 progress-sync → S1 founding-cohort launch (Ch2 @ ₹499 to Heritage pilot) → S2 test engine + parent reports → S3 bundles/open access.

### INFRA / DEPLOY
- ✅ **`vercel.json` SPA rewrite** (commit `5533824`): deep links / refreshes (e.g. `/chapter/ch1/section/s1`) were 404ing on Vercel (no fallback to `index.html`); catch-all rewrite fixes it and lets AuthGuard handle expiry → login.
- **Custom domain `historylab.in` (PENDING UDAY — not code):** add in Vercel → Settings → Domains; DNS at registrar = A `@`→`76.76.21.21`, CNAME `www`→`cname.vercel-dns.com`. **Must also whitelist the new domain** in Supabase Auth (Site URL + Redirect URLs) and Google OAuth (authorized origins + the supabase `/auth/v1/callback` redirect) or login breaks. No repo changes needed.

---

## KEY METHODS & TOOLING (recreate as needed — scratchpad scripts do NOT persist)

**Clean figure extraction (canonical):**
```
pdfimages -png -p "<NCERT PDF>" out/img      # filenames: img-<page>-<obj>.png
# Keep the content images; SKIP template layers: 2480x3508 gray + 1894x1894 (QR) + tiny smasks.
# Match each image to its figure by NCERT PAGE NUMBER (figure->page map: pdftotext per page, grep '^Fig\.')
```
NCERT PDFs: `Claude_Projects/History/NCERT_Rise of Nationalism in Europe.pdf` and `.../Nationalism in India.pdf`.
(The old `pdftoppm` page-render+crop method baked page text/QR into figures — that was the root cause of "misaligned dots".)

**Hotspots:** `{x,y}` = **percent of the displayed image**. **Ch1** field order = `x, y, label`; **Ch2** = `label, description, x, y`. After ANY image swap, re-check coords (aspect-ratio changes shift them — that's how the fig-1 Christ/Shattered regression happened).

**Hotspot verification:** overlay a 10% coordinate grid + the current dots on each figure image with PIL, view it, read corrected positions. **Render at full size (~700–1000px wide per figure), not a tiny contact-sheet thumbnail.** Tooltip flips above when `spot.y >= 58`, so dots up to ~92 are fine.

**Local screenshots of the auth-gated app:**
1. Start dev server: `PATH="/Users/udaysharma/.nvm/versions/node/v20.20.0/bin:$PATH" npx vite --port 5180 --host`
2. Temporarily add a DEV-only bypass at the top of `AuthGuard.tsx` (gated to `import.meta.env.DEV`, activated by `?devpreview=1`), then **REMOVE it before committing**:
   ```tsx
   if (import.meta.env.DEV && new URLSearchParams(window.location.search).get('devpreview') === '1') {
     const mockProfile = { id:'dev', name:'Vedansh', email:'dev@example.com', avatar_url:null, role:'student', school:'Dev', class:'10', profile_completed:true, created_at:'' } as Profile
     return <AuthContext.Provider value={{ profile: mockProfile, signOut: async()=>{} }}>{children}</AuthContext.Provider>
   }
   ```
3. Screenshot: `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --disable-gpu --hide-scrollbars --virtual-time-budget=6000 --window-size=1280,2000 --screenshot=out.png "http://localhost:5180/chapter/ch1?devpreview=1"`
   (mobile: `--window-size=390,2400`). Routes: `/`, `/chapter/ch1`, `/chapter/ch1/section/s1`, `/chapter/ch1/{timeline,maps,figures,flashcards,exam}`. Narrative reading view is internal state (not URL-addressable). Node 20 required.

**Build / deploy:**
```
PATH="/Users/udaysharma/.nvm/versions/node/v20.20.0/bin:$PATH" npm run build   # tsc + vite
# Deploy: on dev -> commit -> git checkout main && git merge dev && git push origin main && git checkout dev
```
Commit co-author: `Co-Authored-By: Claude <noreply@anthropic.com>`.

---

## KEY FILES
- Content: `src/data/ch1/sections/section{1-6}-*.ts`, `src/data/ch2/sections/section{1-4}-*.ts`
- Figures (images + hotspots + analysis): `src/data/ch1/figures.ts`, `src/data/ch2/figures.ts`; images in `public/images/` (ch1) and `public/images/ch2/`
- Narrative card renderer (text/vocab/source/figure/exam-prep/timeline-ref/map-ref/**flowchart**/**table**): `src/components/narrative/NarrativeCard.tsx`
- Hotspot overlay (tooltip flip logic): `src/components/figure/FigureHotspotOverlay.tsx`
- Map markers: `src/components/map/InteractiveMap.tsx`
- Chapter home (Study Tools): `src/modules/HomePage.tsx` · Section page: `src/modules/SectionModule.tsx`
- Theme mockups: `mockups/*.html` (implemented). Theme tokens live in `src/styles/globals.css` `@theme`.
- Card types: `src/types/chapter.ts` (`FlowStep`, `TableData`, `NarrativeCard.steps`, `.table`)
- Reading view + quiz gating: `src/modules/NarrativeMode.tsx` (renders card body + inline quiz); `src/components/narrative/InlineQuizCard.tsx`. **Cards with both a rich body and a quiz now render both — don't revert to the quiz-only swap.**

## CONTENT WORK ALREADY SHIPPED (Neha review Batches 1–8 + figure round)
See `CONTENT_REVIEW_PROCESS.md` for the full log. Summary: NCERT-aligned headings, vocab-before-use, flowcharts (French measures, Napoleonic Code, Zollverein, Treaty of Vienna, Germany/Italy/Britain unification, German 1848 demands, Frankfurt, Satyagraha, 3 satyagrahas), tables (Habsburg, society, NCM participants), Marianne symbols, Greek-war/Balkans exam cards, MCQ answer rebalance; Ch2 WWI-to-chapter-start + Rowlatt reframe + martial law + Jallianwala/Dyer figures; ALL 35 figures re-extracted clean + hotspots remapped; "Study Tools" relabel; section "Start Reading" CTA reorder.
